<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.uwo.crystalli.jukebox.ConnectionActivity">



    <item
        android:id="@+id/guest_icon"
        android:title="@string/guest"
        android:layout_gravity="left"
        android:orderInCategory="50"
        android:icon="@drawable/ic_guest"
        app:showAsAction="always"
        />
        <!--Host ID, should always be in the overflow-->
    <item android:id="@+id/guest_text"
        android:title="@string/guest_text"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"/>

</menu>
